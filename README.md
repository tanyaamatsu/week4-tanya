# 📋 Task Manager API

Проект представляет собой серверную часть (backend) для управления личными задачами. Приложение обеспечивает регистрацию пользователей, аутентификацию через JWT-токены и полный цикл CRUD-операций (создание, чтение, обновление, удаление) для задач.

## 🛠 Технологический стек

* **Фреймворк:** FastAPI (высокая производительность, автоматическая документация Swagger)
* **База данных:** PostgreSQL
* **ORM:** SQLAlchemy (взаимодействие с БД через объекты Python)
* **Аутентификация:** JWT (JSON Web Tokens), passlib (хэширование паролей)
* **Контейнеризация:** Docker, Docker Compose

## 🚀 Как запустить проект

Для запуска приложения необходим установленный **Docker** и **Docker Compose**.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <ссылка_на_ваш_репозиторий>
    cd <имя_папки_проекта>
    ```

2.  **Запустите контейнеры:**
    ```bash
    docker-compose up --build
    ```

3.  **Используйте приложение:**
    * API Документация (Swagger UI): `http://localhost:8000/docs`
    * Фронтенд (если реализован): `http://localhost:8000`

## 📂 Структура проекта

* `app/` — Исходный код приложения (логика, модели, схемы).
* `frontend/` — Файлы пользовательского интерфейса (HTML, JS).
* `Dockerfile` — Инструкция по сборке контейнера приложения.
* `docker-compose.yml` — Оркестрация контейнеров (приложение + БД).
* `requirements.txt` — Список зависимостей Python.