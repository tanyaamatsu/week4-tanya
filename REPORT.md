# 📝 Итоговый отчет по учебной практике

**Студент:** Татьяна
**Тема:** Разработка системы управления задачами (Task Manager)

## 1. Введение
Приложение "Task Manager" разработано для решения задач личного планирования. Основная цель проекта — создание безопасного API, позволяющего пользователям регистрироваться, хранить свои задачи и управлять ими. Проект обеспечивает разграничение прав доступа: пользователь может видеть и редактировать только свои собственные задачи.

## 2. Технологический стек
Для реализации проекта были выбраны следующие технологии:
* **FastAPI:** Выбран благодаря высокой скорости работы, асинхронности и встроенной поддержке валидации данных через Pydantic.
* **PostgreSQL:** Использована в качестве надежной реляционной базы данных для долгосрочного хранения информации о пользователях и задачах.
* **Docker:** Применен для обеспечения переносимости приложения. Это позволило упаковать приложение и базу данных в контейнеры, гарантируя идентичную работу на любом окружении.

## 3. Ход работы
* **Неделя 1:** Проектирование архитектуры базы данных, настройка окружения Docker, написание `Dockerfile` и `docker-compose.yml`.
* **Неделя 2:** Реализация моделей SQLAlchemy, настройка миграций и базовых CRUD-операций для задач.
* **Неделя 3:** Реализация системы безопасности: хэширование паролей (bcrypt), генерация и проверка JWT-токенов.
* **Неделя 4:** Настройка CORS для работы с фронтендом, финальное тестирование и подготовка отчетной документации.

## 4. Сложности
Самой серьезной проблемой стала настройка **CORS (Cross-Origin Resource Sharing)**. Браузер блокировал запросы из фронтенда (`index.html`) к серверу (`localhost:8000`), выдавая ошибку безопасности. Проблема была решена добавлением `CORSMiddleware` в FastAPI, что позволило разрешить входящие запросы с локального хоста.
Также потребовалось время на понимание работы JWT-токенов, особенно в части привязки задач к конкретному `user_id` после декодирования токена.

## 5. Результаты

### 5.1. Документация API (Swagger UI)
На скриншоте показан процесс авторизации (получения токена) и успешный ответ сервера (код 200).

`[ВСТАВИТЬ СКРИНШОТ: Swagger UI - успешный логин /token]`

### 5.2. Тестирование защищенного запроса
Скриншот демонстрирует работу эндпоинта `GET /tasks`, который возвращает список задач только после предоставления корректного токена.

`[ВСТАВИТЬ СКРИНШОТ: Swagger UI - успешный GET /tasks]`

### 5.3. Фронтенд (Интерфейс пользователя)
На скриншоте представлен рабочий интерфейс, созданный на HTML/JS, подключенный к backend-части.

`[ВСТАВИТЬ СКРИНШОТ: Фронтенд с заполненными задачами]`

## 6. Вывод
В ходе учебной практики я освоила полный цикл разработки backend-приложения: от проектирования структуры базы данных до контейнеризации и деплоя. Я научилась работать с асинхронными фреймворками, реализовывать безопасную аутентификацию и отлаживать взаимодействие между фронтендом и сервером.
Полученный опыт является отличной базой для дальнейшего развития в Backend-разработке.